# Google Maps — crie a chave no Google Cloud (Maps JavaScript API + Geocoding API).
# IMPORTANTE: restrinja a chave por domínio (HTTP referrer) e ponha teto de cota
# (limite ~10k/mês) pra NÃO gerar custo. O Map ID é criado no Google Cloud (grátis).
VITE_GOOGLE_MAPS_API_KEY=
VITE_GOOGLE_MAPS_MAP_ID=

VITE_APP_USERNAME=
VITE_APP_PASSWORD=
