{
  "name": "terrenos-api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend serverless do app Terrenos (Lambda + DynamoDB single-table + Cognito + S3).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed": "tsx scripts/seed.ts",
    "deploy": "serverless deploy",
    "setup:oidc": "./scripts/setup-oidc-provider.sh"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1061.0",
    "@aws-sdk/client-dynamodb": "^3.1061.0",
    "@aws-sdk/client-s3": "^3.1061.0",
    "@aws-sdk/lib-dynamodb": "^3.1061.0",
    "@aws-sdk/s3-presigned-post": "^3.1061.0",
    "@types/aws-lambda": "^8.10.161",
    "@types/node": "^25.9.1",
    "serverless": "^4.37.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
